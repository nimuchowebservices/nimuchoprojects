services:
  - type: web
    name: nimucho-digital
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn nimucho_project.wsgi:application
    envVars:
      - key: DEBUG
        value: "False"